function solution(array, commands) {
    var answer = [];

    // commands에서 어떻게 원소값을 가져올 것인가
    for(let i = 0; i < commands.length; i++) {
        array.slice(commands[])
    }

    return answer;
}