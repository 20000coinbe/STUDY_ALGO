/*
    다익스트라(Dijkstra) : 단일출발 최단경로 (특정 노드를 중심으로 모든 노드까지의 거리계산)
*/

const dijkstra = function(graph, start) {
    
    const distances = [];
    distances.fill("inf", 1);
    distances[0] = 0;

    console.log(distances);
}

